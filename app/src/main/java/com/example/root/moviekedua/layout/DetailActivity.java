package com.example.root.moviekedua.layout;import android.content.Context;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.widget.ImageView;import android.widget.TextView;import com.example.root.moviekedua.R;import com.squareup.picasso.Picasso;import java.text.ParseException;import java.text.SimpleDateFormat;import java.util.Date;public class DetailActivity extends AppCompatActivity {    TextView det_title, det_overview, det_rate, det_release;    ImageView det_poster;    private Context context;    public static String EXTRA_TITLE        = "extra_title";    public static String EXTRA_OVERVIEW     = "extra_overview";    public static String EXTRA_RELEASE      = "extra_release";    public static String EXTRA_POSTER       = "extra_poster";    public static String EXTRA_RATE         = "extra_rate";    public static String EXTRA_RATE_AVG     = "extra_rate_avg";    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_detail);        det_title   = (TextView)findViewById(R.id.detailTitle);        det_release   = (TextView)findViewById(R.id.detailRelease);        det_rate   = (TextView)findViewById(R.id.detailRate);        det_overview   = (TextView)findViewById(R.id.detailOverview);        det_poster   = (ImageView) findViewById(R.id.detailPoster);        //setting title        det_title.setText(getIntent().getStringExtra(EXTRA_TITLE));        //setting overview        String overiew = "Nothing overview data ";        if (!getIntent().getStringExtra(EXTRA_OVERVIEW).isEmpty()){            overiew = getIntent().getStringExtra(EXTRA_OVERVIEW);        }        det_overview.setText(overiew);        //setting release date        String getDate = getIntent().getStringExtra(EXTRA_RELEASE);        SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd");        try {            Date date = dateFormat.parse(getDate);            SimpleDateFormat newDateFormat = new SimpleDateFormat("EEEE, dd MM yyyy");            String showDate = newDateFormat.format(date);            det_release.setText(showDate);        } catch (ParseException e){            e.printStackTrace();        }        //setting rate        det_rate.setText(getIntent().getStringExtra(EXTRA_RATE)+" ("+getIntent().getStringExtra(EXTRA_RATE_AVG)+" / 10)");        //setting backdrop        String poster_path = getIntent().getStringExtra(EXTRA_POSTER);        Picasso.with(context).load("http://image.tmdb.org/t/p/w500/"+poster_path).into(det_poster);    }}